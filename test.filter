Show
    BaseType == "Orb of Alchemy"
    #.import STYLE | gold -> t1

Show
    BaseType == "Chromatic Orb"
    #.import STYLE | gold -> t2

Show
    BaseType == "Orb of Fusing"
    #.import STYLE | gold -> t3

Show
    BaseType == "Chaos Orb"
    #.import STYLE | gold -> t4

Show
    BaseType == "Exalted Orb"
    #.import STYLE | fragment -> t5

Show
    BaseType == "Scroll of Wisdom"
    #.import STYLE | fragment -> catchall

Hide
    BaseType == "Gold"
# Synthesized

Show
    Class == CLASS
    SynthesisedItem True
    DropLevel >= DROP_LEVEL_ARMOUR_BEST
    ItemLevel >= ITEM_LEVEL_BEST
    #.import < _util -> multi_t1

Show
    Class == CLASS
    SynthesisedItem True
    DropLevel >= DROP_LEVEL_ARMOUR_BEST
    #.import < _util -> multi_t2

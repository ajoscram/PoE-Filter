Show #.name t1
    #.import < template > magic -> t1 { ICON_TEMPLATE = ICON_WEAPON

Show #.name t2
    #.import < template > magic -> t2 { ICON_TEMPLATE = ICON_WEAPON

Show #.name t3
    #.import < template > magic -> t3 { ICON_TEMPLATE = ICON_WEAPON

Show #.name t4
    #.import < template > magic -> t4

Show #.name t5
    #.import < template > magic -> t5